a = (1, 7, 2, 1, 1)
# print(a.count(1))  # This counts the number of times the given is occured inside
# a tuple. We use this to find the numbere of times the given integr is repeated
print(a.index(7))  # This is used to find the the first occuring index value of
# given integr inside the (7).
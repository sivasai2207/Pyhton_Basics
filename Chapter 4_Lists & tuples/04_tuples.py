# myTuple = (9, 4, 3, 7)
# print(myTuple)
# print(type(myTuple))
myTuple = ()  # Here we can also create an empty tuple.
print(myTuple)
print(type(myTuple))

myTuple = (4, )  # if the tuple has one element we should add comma
# at the end of th tuple or else it will consider as assignment of integer and
# changes the value of the given input to an int. so comma inside a tuple is needed.
print(myTuple)
print(type(myTuple))
